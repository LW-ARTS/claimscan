---
phase: 12.1-splitvault-handler-implement-third-flap-vault-type-3979-inst
plan: 01
subsystem: test-scaffolding
tags: [vitest, it-todo, wave-0, tests]
requires: [phase-12-complete]
provides:
  - SV-02 unit test stubs (3 it.todo in split-vault.test.ts)
  - SV-04 unit test stubs (2 it.todo in flap-vaults.test.ts)
  - SV-08 integration test stub (1 it.todo in flap.test.ts)
  - vaultType assertion array widened to include 'split-vault'
affects: [Plan 12.1-04 drives split-vault tests, Plan 12.1-05 drives flap-vaults tests, Plan 12.1-07 drives integration test]
tech-stack:
  added: []
  patterns:
    - "vi.hoisted + readContractMock + captureMessageMock (existing flap-vaults.test.ts L1-37 harness)"
    - "describe.skipIf(!process.env.BSC_RPC_URL) for integration-only tests"
    - "it.todo placeholders for RED test surface, driven to GREEN by subsequent plans"
key-files:
  created:
    - lib/__tests__/unit/split-vault.test.ts
  modified:
    - lib/__tests__/unit/flap-vaults.test.ts
    - lib/__tests__/integration/flap.test.ts
decisions:
  - "Used it.todo (not expect.fail) per Phase 12.1 plan — vitest reports todo as benign, not failure. RED→GREEN happens in Plans 04, 05, 07"
  - "Widened vaultType assertion array in existing test from ['base-v1','base-v2','unknown'] to include 'split-vault' BEFORE Plan 03's TS union widening — runtime string array is structural, not nominal, so safe"
  - "Reused existing fixture constants (VAULT_PORTAL, VAULT_ADDRESS, USER) in split-vault.test.ts — opaque since bscClient is mocked"
metrics:
  completed: "2026-04-26"
  tasks_completed: 2
  tasks_total: 2
  files_created: 1
  files_modified: 2
  tests_added: 6 (3 in split-vault.test.ts + 2 in flap-vaults.test.ts + 1 in flap.test.ts)
commit: 7cc4476
---

# Phase 12.1 Plan 01: Wave 0 Test Stubs

Created Wave 0 RED test scaffolding so Plans 04, 05, 07 can drive each surface from todo to GREEN as implementation lands.

## What Was Built

1. **NEW: `lib/__tests__/unit/split-vault.test.ts`** with 3 `it.todo` stubs covering tuple decode, steady-state (zero claimable), and error path. Uses verbatim hoisted-mock harness from existing flap-vaults.test.ts L1-37.
2. **EXTENDED: `lib/__tests__/unit/flap-vaults.test.ts`** with 2 `it.todo` for SplitVault probe routing (4th probe in resolveVaultKind ladder) and HANDLERS dispatch (`resolveHandler('split-vault')`).
3. **EXTENDED: `lib/__tests__/integration/flap.test.ts`** with 1 `it.todo` for SplitVault parity (find-one-at-runtime pattern). Also widened the existing test's assertion array from `['base-v1', 'base-v2', 'unknown']` to `['base-v1', 'base-v2', 'split-vault', 'unknown']`.

`npm run test:unit -- split-vault flap-vaults` runs cleanly with todo entries pre-implementation.

## Verification

- File `lib/__tests__/unit/split-vault.test.ts` exists with `vi.hoisted` mock harness and 3 `it.todo`
- `grep -c "it.todo" lib/__tests__/unit/flap-vaults.test.ts` returns 2 new entries
- `grep -c "it.todo" lib/__tests__/integration/flap.test.ts` returns 1 new entry
- `grep -q "'base-v1', 'base-v2', 'split-vault', 'unknown'" lib/__tests__/integration/flap.test.ts`
- `npx tsc --noEmit` exits 0
