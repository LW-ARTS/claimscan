---
phase: 12.1-splitvault-handler-implement-third-flap-vault-type-3979-inst
plan: 06
subsystem: scripts
tags: [scripts, classify, drift-mirror, wave-4]
requires: [Plan 12.1-05]
provides:
  - scripts/classify-flap.ts probe ladder mirrors lib/platforms/flap-vaults/index.ts (Pitfall 2 mitigation)
  - splitVault counter + progress log + final summary line in script
  - drift parity grep verifies both files contain SplitVault probe
affects: [Plan 12.1-08 task 2 runs this script against production to reclassify ~3979 unknown rows to split-vault]
tech-stack:
  added: []
  patterns:
    - "Standalone tsx script with dotenv-loaded BSC_RPC_URL + Supabase service-role client (Phase 12 pattern from classify-flap original)"
    - "Probe ladder duplicated (not imported) — script must work standalone, library has 'server-only' import that breaks Node-CLI execution"
    - "Atomic 7-touchpoint update (probe insertion, counter decl, counter increment, progress log, final summary, type widening)"
key-files:
  created: []
  modified:
    - scripts/classify-flap.ts
decisions:
  - "Mirrored probe in same slot as lib (after V1, before unknown) — keeps the two ladders semantically identical"
  - "All 7 touchpoints applied in single commit: VAULT_CATEGORY_MAP type, function return type, probe block insertion, counter declaration, counter increment, progress log line, final summary line"
  - "Drift parity check: `grep -q userBalances lib/platforms/flap-vaults/index.ts && grep -q userBalances scripts/classify-flap.ts` — both pass"
  - "Did NOT extract a shared probe module — script's `import 'server-only'` from lib would break tsx execution. Duplication is intentional, drift is mitigated by the parity grep + tests (Plan 05 unit + Plan 07 integration both validate the lib path)"
metrics:
  completed: "2026-04-26"
  tasks_completed: 1
  tasks_total: 1
  files_created: 0
  files_modified: 1
commit: 6929093
---

# Phase 12.1 Plan 06: classify-flap.ts Probe Ladder Mirror

Mirrored the new SplitVault probe into the standalone classification script so the post-deploy reclassification run picks up split-vault rows correctly.

## What Was Built

`scripts/classify-flap.ts` updated in 7 touchpoints (per PATTERNS.md L833+):
1. `VAULT_CATEGORY_MAP` (if used) — annotation
2. Function return type — `Promise<'base-v1' | 'base-v2' | 'split-vault' | 'unknown'>`
3. Probe block insertion (after V1, before unknown terminal)
4. Counter declaration: `let splitVaultCount = 0;`
5. Counter increment in dispatch switch: `else if (kind === 'split-vault') splitVaultCount++;`
6. Progress log line includes `splitVault=${splitVaultCount}` token
7. Final summary line: `console.log('  split-vault: ${splitVaultCount}');`

## Verification

- `grep -q "split-vault" scripts/classify-flap.ts` → match
- `grep -q "userBalances" scripts/classify-flap.ts` → match
- `grep -q "splitVaultCount" scripts/classify-flap.ts` → match
- `grep -q "userBalances" lib/platforms/flap-vaults/index.ts` → match (Pitfall 2 drift parity ✓)
- `npx tsc --noEmit` → exit 0
